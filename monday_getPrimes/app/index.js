'use strict';

let getPrimes = (number) => {
  if (number <= 0)
    return "invalid number";

	let primes = [];

	for (let i = 0; i < number; i++) {
		
	}
	return primes;
}

module.exports = getPrimes